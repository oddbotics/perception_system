cmake_minimum_required(VERSION 2.8)
project( BoTracker )
find_package( OpenCV )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( BotTracker bot_tracker.cpp )
target_link_libraries( BotTracker ${OpenCV_LIBS} )
